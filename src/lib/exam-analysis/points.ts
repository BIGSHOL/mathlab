/**
 * @deprecated 경로 호환용 배럴 — 실체는 `./shared/points`.
 * 수학·영어 파이프라인 분리(2026-08) 중 UI 쪽 import 경로를 흔들지 않으려고 남겨둔 임시 파일.
 * 모든 import 사이트가 `@/lib/exam-analysis/shared/points` 로 옮겨지면 삭제할 것.
 */
export * from './shared/points';
