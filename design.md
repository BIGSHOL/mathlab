# MathLAB 기출분석 — 랜딩페이지 디자인 명세 (design.md)

> 목적: 루트(`/`)에 들어올 방문자에게 **기출분석 제품의 가치**를 전달하고 로그인/도입 문의로 전환시키는 공개 랜딩페이지. 현재 `/`는 `/exam-analysis` 로 redirect 중이므로 이 명세대로 **랜딩 페이지로 교체**한다. 본 문서는 Claude(또는 개발자)가 그대로 구현할 수 있는 수준의 스펙이다.

---

## 0. 컨텍스트 (이 제품이 무엇인가)

mathlab 은 **한국 수학 학원용 "기출(시험지) 분석" 도구**다. 핵심 흐름:

```
시험지 PDF 업로드  →  AI 자동 분석(Gemini 비전 OCR + Claude)  →  결과 리포트
                                                              ├ 난이도 분포 / 단원·유형 분석
                                                              ├ 문항별 해설 + 총평 자동 생성
                                                              ├ 주변 학교 기출 비교(내신 대비)
                                                              ├ 도넛/레이더 차트 자동 렌더
                                                              └ 네이버 블로그용 분석 이미지 생성(학원 마케팅)
```

- **타깃**: 학원 원장·수학 선생님 (B2B). 멀티테넌트(지점별).
- **이미 가진 자산**: Next.js 15 / React 19 / Tailwind v4, KaTeX(수식), 한글 폰트(Pretendard + Noto Serif KR), indigo 기반 디자인 토큰(`globals.css`).
- **랜딩의 일**: "시험지만 올리면 분석·해설·블로그 콘텐츠가 자동으로" 라는 시간 절약 + 전문성 + 마케팅 가치를 빠르게 납득시키고 → **로그인**(기존 학원) 또는 **도입 문의**(신규)로.

---

## 1. 한 줄 정의 & 포지셔닝

- **헤드라인 컨셉**: "시험지 한 장이면, 분석부터 블로그 글까지 자동으로."
- **포지셔닝**: 손으로 며칠 걸리던 기출 분석·해설·내신 대비 자료를 **AI가 수 분 안에** — 학원의 전문성과 마케팅을 동시에.
- **톤**: 신뢰감 있는 + 모던한 B2B SaaS. 과장된 마케팅 X, "정확/전문/빠름" 강조.

---

## 2. 정보 구조 (섹션 순서)

1. **헤더(고정)** — 로고, (앵커 네비), 로그인 버튼
2. **히어로** — 헤드라인 + 서브카피 + 1차 CTA + 제품 시각물(분석 화면/차트 mockup)
3. **신뢰 스트립** — 한 줄 지표/사용 학원 로고(있으면) 또는 "AI 기반 · 한국 교육과정 특화" 배지
4. **핵심 가치 3~4** — 카드: ① 자동 분석 ② 난이도·단원 인사이트 ③ 해설·총평 생성 ④ 블로그 콘텐츠
5. **작동 방식 (3단계)** — 업로드 → AI 분석 → 리포트·공유
6. **기능 상세** — 난이도 차트 / 유형·킬러문항 / 문항 해설 / 주변학교 비교 / 네이버 블로그 이미지 / 문제은행 추출 (아이콘 + 짧은 설명, 2열 그리드 또는 좌우 교차 스크롤)
7. **마무리 CTA** — "지금 시작하기 / 도입 문의" (강조 배경)
8. **푸터** — 서비스명, 문의(이메일), (약관/개인정보 링크 있으면)

> 최소 버전(MVP)이면 1·2·4·5·7·8 만으로도 성립. 3·6 은 콘텐츠가 준비되면 추가.

---

## 3. 비주얼 시스템 (기존 토큰 재사용 — 새 색 만들지 말 것)

`src/app/globals.css` 의 `@theme` / `:root` 토큰을 그대로 사용한다.

| 용도 | 토큰 / 값 |
|---|---|
| Primary | `--primary` (#3B5BDB indigo) / hover #4338CA |
| Secondary(포인트) | `--color-secondary` (#F97316 orange) — CTA 보조·강조 최소 사용 |
| 본문 텍스트 | `--ink` (#0F172A), 보조 `--ink-2`, 흐림 `--ink-3` |
| 라인/보더 | `--line` (#E2E8F0) |
| 배경 | `--bg` / 흰색 카드 `--bg-2` |
| 그림자 | `--shadow-card`, hover 시 `--shadow-card-md` |
| 라운드 | 카드 `--r-lg`(16px), 버튼 `--r`(12px) |

- **타이포그래피**: 본문/UI = Pretendard(이미 로드됨). 큰 헤드라인은 Pretendard 800(black). 수식 샘플은 KaTeX. 숫자 강조는 굵게.
  - H1(히어로): 40~56px / 800 / letter-spacing -0.02em
  - H2(섹션 제목): 28~32px / 800
  - 본문: 16~18px / 1.7 line-height / `--ink-2`
- **간격**: 섹션 상하 패딩 96px(데스크톱)/56px(모바일). 콘텐츠 최대폭 `max-width: 1120~1200px`, 좌우 24px 패딩.
- **모티프**: 수학 그리드 배경(`.math-grid-bg` 유틸 활용)·은은한 수식($\frac{}{}$, $\int$, 도형) 워터마크. 히어로 배경에 `radial-gradient(circle at top, var(--primary-50), var(--bg))` (점검 페이지에서 쓰던 패턴 재사용 가능).
- **모션**: 스크롤 진입 시 fade-up(8~16px) — `framer-motion`(이미 설치됨) 또는 IntersectionObserver. 과하지 않게, 한 번만. 버튼·카드 hover 살짝 상승(`-translate-y-0.5`) + 그림자 강화(기존 Button 패턴과 동일).

---

## 4. 섹션별 상세

### 4.1 헤더 (sticky, 투명→스크롤 시 흰 배경)
- 좌: 로고(`LogoIcon` + "MathLAB 기출분석" 또는 지점 로고). 
- 우: 앵커("기능", "작동 방식") + **로그인** 버튼(primary, sm).
- 스크롤 0px일 땐 투명/히어로 위에 올라타고, 스크롤되면 흰 배경 + 하단 `--line` 보더 + 그림자. height 64px.

### 4.2 히어로
- 레이아웃: 데스크톱 좌(텍스트 55%)·우(시각물 45%) 2단, 모바일 1단(텍스트→시각물).
- 텍스트:
  - **H1**: "시험지 한 장이면, 분석부터 블로그 글까지 자동으로"
  - **서브**: "PDF만 올리면 AI가 난이도·단원·문항 해설·총평을 만들고, 네이버 블로그용 자료까지 생성합니다." (2줄, `--ink-2`)
  - **CTA 영역**: [시작하기](primary) + [기능 보기](ghost, 앵커 스크롤). 
    - **auth-aware**: 로그인 상태면 1차 CTA = "기출분석 바로가기"(→`/exam-analysis`), 비로그인이면 "로그인"(→`/login`). (구현 노트 §8)
  - 하단 작은 신뢰 문구: "AI 기반 · 한국 수학 교육과정 특화".
- 시각물(우): **실제 분석 결과 화면 mockup** — 난이도 도넛 차트 + 문항 카드 + 총평 일부를 담은 카드(흰 배경, `--shadow-card-lg`, 살짝 회전/겹침). 실제 스크린샷이 있으면 그걸, 없으면 더미 차트(도넛 + 막대) SVG.
  - 부유 요소: 작은 "분석 완료" 배지, "난이도 ★★★☆" 칩 등으로 생동감.

### 4.3 신뢰 스트립 (선택)
- 한 줄: "이미 N개 학원이 사용 중" 또는 사용 학원 로고 그레이스케일 나열. 데이터 없으면 → "Gemini · Claude AI 기반", "한국 중·고 수학 교육과정 매핑", "내신/모의고사 기출" 배지 3개.

### 4.4 핵심 가치 (카드 3~4)
2×2 또는 4열 그리드. 각 카드: 아이콘(lucide) + 제목 + 1~2줄.
1. **자동 기출 분석** — "시험지 PDF 업로드 한 번. OCR·문항 추출·난이도 판정까지 AI가." (icon: FileSearch)
2. **난이도·단원 인사이트** — "단원별 출제 비중, 난이도 분포, 킬러문항 패턴을 차트로." (icon: BarChart3)
3. **해설·총평 자동 생성** — "문항별 풀이 해설과 시험 총평을 즉시. 검토만 하면 끝." (icon: FileText)
4. **블로그 콘텐츠 자동화** — "분석 결과를 네이버 블로그용 이미지로 한 번에. 학원 홍보까지." (icon: ImageDown / Share2)

### 4.5 작동 방식 (3 STEP)
가로 3단(모바일 세로). 번호 + 아이콘 + 제목 + 설명, 사이 연결선/화살표.
1. **업로드** — "시험지 PDF를 끌어다 놓기." (Upload)
2. **AI 분석** — "수 분 내 난이도·단원·해설·총평 자동 생성." (Sparkles/Cpu) — 진행 단계 시각화 가능
3. **리포트 & 공유** — "차트 리포트 확인, 인쇄·PDF·네이버 블로그 이미지로 공유." (Share2)

### 4.6 기능 상세 (좌우 교차 또는 2열)
각 기능: 좌측 설명(제목+불릿 2~3) / 우측 시각물(스크린샷·차트), 행마다 좌우 교차.
- **난이도·점수 차트** — 도넛(난이도 분포) + 막대(배점) + 레이더(유형). (실제 resvg 차트 이미지 활용 가능)
- **단원·유형·킬러문항** — 단원별 출제 비중, 자주 나오는 유형, 킬러문항 식별.
- **문항별 해설 & 총평** — KaTeX 수식 렌더된 해설 카드 예시 1개 실제 표시($\frac{...}{...}$ 포함).
- **주변 학교 비교** — "우리 지역 학교 기출과 난이도·단원 비교로 내신 대비." (지도/학교 칩)
- **네이버 블로그 이미지** — "클릭 한 번으로 블로그 게시용 분석 이미지 세트 생성."
- **문제은행 추출** — "분석한 문항을 문제은행으로 추출(선택)."

### 4.7 마무리 CTA
- 배경: primary 그라데이션 또는 `--primary-50` 패널, 라운드 큰 카드.
- 카피: "기출 분석, 이제 AI에게 맡기세요" + 버튼 [로그인 / 시작하기] + 보조 "도입 문의"(mailto 또는 문의 링크).

### 4.8 푸터
- 좌: 로고 + 한 줄 소개. 우: 문의 이메일(`chrismathone@gmail.com` — 기존 점검 페이지에 있던 값), (약관/개인정보처리방침 링크는 페이지 있으면). © 연도 Injaewon MathLAB.

---

## 5. 카피 초안 (한국어, 교체 가능)

- 히어로 H1: **"시험지 한 장이면, 분석부터 블로그 글까지 자동으로"**
- 히어로 서브: "PDF만 올리면 AI가 난이도·단원·문항 해설·총평을 만들고, 네이버 블로그용 자료까지 생성합니다."
- 가치 섹션 제목: "선생님의 시간을 돌려드립니다"
- 작동 방식 제목: "3단계면 충분합니다"
- 마무리 CTA: "기출 분석, 이제 AI에게 맡기세요"

> 카피는 초안. 실제 지표(분석 시간, 사용 학원 수)가 있으면 숫자로 교체하면 설득력 ↑.

---

## 6. 컴포넌트 & 인터랙션

- **재사용**: `@/components/ui/Button`(primary/secondary/ghost), `LogoIcon`. 새 UI는 섹션 전용으로 가볍게.
- **버튼**: 기존 Button 컴포넌트 그대로 (variant/size). CTA는 `size="lg"`.
- **스크롤 앵커**: 헤더의 "기능/작동 방식" → 해당 섹션으로 smooth scroll.
- **모션**: 섹션별 fade-up 1회. 히어로 시각물은 살짝 떠 있는(float) 애니메이션 옵션.
- **상호작용 최소**: 랜딩은 정보 전달 위주, 무거운 인터랙션 지양(성능·신뢰).

---

## 7. 반응형

- 브레이크포인트: 모바일 < 768 / 태블릿 768~1024 / 데스크톱 ≥ 1024.
- 히어로: 데스크톱 2단 → 모바일 1단(텍스트 먼저, 시각물 아래).
- 카드 그리드: 4열 → 2열(태블릿) → 1열(모바일).
- 기능 상세 좌우 교차: 모바일에선 항상 시각물 위·텍스트 아래로 통일.
- 헤더: 모바일은 앵커 숨기고 로고 + 로그인만.
- 터치 타깃 ≥ 44px, 폰트 최소 15px.

---

## 8. 구현 노트 (Claude/개발자용)

- **파일**: `src/app/page.tsx` 의 `redirect('/exam-analysis')` 를 **랜딩 페이지로 교체**. 섹션이 길면 `src/components/landing/*` 로 분리(Hero.tsx, Features.tsx, HowItWorks.tsx, CTA.tsx, Footer.tsx) 후 page.tsx에서 조합.
- **공개 접근**: `/` 는 루트 레이아웃(`app/layout.tsx`, SessionProvider+폰트+globals.css) 하위이고 (teacher) 레이아웃 밖이라 **인증 불필요**(미들웨어도 차단 안 함). 비로그인 방문자에게 그대로 노출 OK.
- **auth-aware CTA**: 클라이언트에서 `useAuth()` 로 세션 확인 → 로그인 상태면 "기출분석 바로가기"(`/exam-analysis`), 아니면 "로그인"(`/login`). 헤더 버튼도 동일. (히어로/헤더만 `'use client'`, 나머지는 서버 컴포넌트로 둬 성능 확보)
- **폰트/스타일**: 새 폰트·색 추가 금지 — 기존 토큰/Pretendard 사용. 수식은 KaTeX(이미 CDN 로드).
- **차트/시각물**: 실제 제품 스크린샷이 가장 설득력 ↑. 없으면 더미 SVG(도넛/막대/레이더). resvg 차트 이미지(`/api/exam-analysis/[id]/chart`)는 인증/데이터 필요하므로 랜딩엔 정적 이미지 권장.
- **성능**: 가능한 정적/서버 렌더. 이미지 `next/image` + lazy. framer-motion은 모션 쓰는 컴포넌트만 client. LCP(히어로 헤드라인/이미지) 최적화.
- **SEO/메타**: `app/page.tsx` 또는 `layout` 의 `metadata` 에 title/description/og 설정. 한글 description. (현재 점검용 metadata 교체)
- **다국어**: 한국어 단일.

---

## 9. 접근성

- 시맨틱 마크업: `<header><main><section><footer>`, 섹션마다 `<h2>`.
- 색 대비 AA 이상(특히 `--ink-2` on 흰/연배경).
- 모든 이미지 `alt`, 아이콘 장식은 `aria-hidden`.
- 키보드 포커스 링(기존 `focus:ring-primary/40` 패턴), 모션 줄이기(`prefers-reduced-motion`) 존중.

---

## 10. 가정 & 결정 필요 (구현 전 확인하면 좋은 것)

이 명세는 다음을 **가정**했다 — 다르면 알려주면 반영:
1. **브랜드명**: "MathLAB 기출분석" / "Injaewon MathLAB" (로그인 페이지 기준). 별도 제품명 있으면 교체.
2. **CTA 전략**: 1차 = 로그인(기존 학원), 보조 = 도입 문의(mailto). 신규 가입 폼/데모 신청 흐름이 따로 있으면 그쪽으로.
3. **신뢰 요소**: 사용 학원 수·로고·후기 등 실제 데이터 유무(없으면 "AI 기반/교육과정 특화" 배지로 대체).
4. **시각물**: 실제 분석 화면 스크린샷 제공 가능 여부(권장) vs 더미 그래픽.
5. **범위**: MVP(히어로+가치+작동방식+CTA) 먼저 vs 전체 섹션 한 번에.

---

## 11. 구현 순서 제안 (착수 시)

1. `src/components/landing/` 에 Hero / Features / HowItWorks / CTA / Footer 작성 (토큰·Button 재사용).
2. `app/page.tsx` 를 이들 조합으로 교체(redirect 제거). 메타데이터 갱신.
3. auth-aware CTA 배선(`useAuth`).
4. 반응형·모션·접근성 패스.
5. `npm run build` + 데스크톱/모바일 시각 검증(Chrome MCP). 
6. 배포 후 LCP/대비 점검.
